% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rho_overlap_sensitivity.R
\name{rho_overlap_sensitivity}
\alias{rho_overlap_sensitivity}
\title{Sensitivity of corrected association to assumed measurement overlap}
\usage{
rho_overlap_sensitivity(
  r_obs,
  vary = c("multiplier", "common", "item"),
  focal_scale = c("A", "B"),
  focal_item = NULL,
  grid = seq(0, 2, by = 0.01),
  type = c("continuous", "ordinal"),
  measurement_model,
  cA = 0,
  cB = 0,
  k_overlap = 0,
  overlapA = NULL,
  overlapB = NULL,
  cA_vec = NULL,
  cB_vec = NULL,
  thrA = NULL,
  thrB = NULL,
  threshold_scale = c("z", "raw"),
  scoresA = NULL,
  scoresB = NULL,
  ci = c("analytic", "bootstrap", "none"),
  r_SE = NULL,
  r_ci = NULL,
  n_boot = 0,
  conf_level = 0.95,
  return_fits = FALSE,
  warn_boundary = TRUE,
  show_progress = interactive(),
  progress_callback = NULL,
  ...
)
}
\arguments{
\item{r_obs}{Observed total-score correlation. Must be strictly between -1 and 1.}

\item{vary}{Character string indicating how overlap is varied. \code{"multiplier"}
multiplies the supplied overlap values by each grid value. \code{"common"}
replaces all non-zero overlap values by the grid value. \code{"item"} varies one
focal item-specific cross-loading.}

\item{focal_scale}{Character string. Either \code{"A"} or \code{"B"}. Only used when
\code{vary = "item"} and indicates whether an item-specific cross-loading in
\code{cA_vec} or \code{cB_vec} is varied.}

\item{focal_item}{Integer. Item index to vary when \code{vary = "item"}.}

\item{grid}{Numeric vector of overlap values or multipliers to evaluate.}

\item{type}{Character string. Either \code{"continuous"} for continuous item scores
or \code{"ordinal"} for ordinal item scores.}

\item{measurement_model}{A measurement model specification created with
\code{reliability_model()}, \code{tau_model()}, \code{parallel_model()}, or
\code{congeneric_model()}.}

\item{cA, cB}{Scalar cross-loading values for overlapping items from scale A on
construct B and from scale B on construct A. Used when item-specific vectors
are not supplied.}

\item{k_overlap}{Number of overlapping items per scale when using scalar
cross-loading values.}

\item{overlapA, overlapB}{Optional integer vectors identifying overlapping items
in scale A and scale B.}

\item{cA_vec, cB_vec}{Optional item-specific cross-loading vectors for scale A
and scale B.}

\item{thrA, thrB}{Threshold vectors for ordinal items. Required when
\code{type = "ordinal"}.}

\item{threshold_scale}{Scale of the thresholds. Either \code{"z"} for thresholds on
the standard-normal latent-response scale or \code{"raw"} for raw thresholds.}

\item{scoresA, scoresB}{Optional score values assigned to ordinal response
categories.}

\item{ci}{Confidence interval method. \code{"analytic"} applies the correction to
the confidence limits of the observed correlation. \code{"bootstrap"} uses a
parametric Fisher-z bootstrap. \code{"none"} returns only point estimates.}

\item{r_SE}{Standard error of the observed total-score correlation. Required
for \code{ci = "bootstrap"} and used for \code{ci = "analytic"} when \code{r_ci} is not
supplied.}

\item{r_ci}{Optional numeric vector of length 2 giving the lower and upper
confidence limits of the observed total-score correlation. Can be used with
\code{ci = "analytic"} instead of \code{r_SE}.}

\item{n_boot}{Number of bootstrap draws. Only used when \code{ci = "bootstrap"}.}

\item{conf_level}{Confidence level for analytic or bootstrap confidence
intervals.}

\item{return_fits}{Logical. If \code{TRUE}, returns the full correction object for
each grid value.}

\item{warn_boundary}{Logical. If \code{TRUE}, boundary-related information is
retained. Warnings from individual grid evaluations are suppressed to avoid
repeated messages.}

\item{show_progress}{Logical. If \code{TRUE}, shows a text progress bar.}

\item{progress_callback}{Optional function called at each grid value as
\code{progress_callback(i, n)}.}

\item{...}{Additional arguments passed to \code{\link[=rho_correction]{rho_correction()}}.}
}
\value{
An object of class \code{"rho_overlap_sensitivity"} with the following
elements:
\describe{
\item{critical_overlap}{The first grid value at which the confidence
interval includes zero, if available.}
\item{observed_overlap}{The grid value closest to 1, representing the
originally assumed overlap under \code{vary = "multiplier"}.}
\item{baseline_overlap_value}{The non-zero baseline overlap value when a
single common value can be inferred.}
\item{ci_includes_zero_at_observed}{Logical indicating whether the
confidence interval includes zero at the observed overlap value.}
\item{results}{A data frame with one row per grid value, including the
corrected estimate, confidence interval limits, boundary information, and
zero-inclusion indicators.}
\item{fits}{If \code{return_fits = TRUE}, a list of full correction results.}
}
}
\description{
Varies the assumed measurement-overlap intensity and recomputes the corrected
latent association at each value of a user-specified grid. This is useful when
the degree of overlap is uncertain and researchers want to evaluate how robust
their corrected association is to alternative overlap assumptions.
}
\details{
The function repeatedly calls \code{\link[=rho_correction]{rho_correction()}} while changing the assumed
overlap intensity. With \code{vary = "multiplier"}, the grid is interpreted as a
multiplier applied to the originally supplied overlap values. For example,
when \code{cA = cB = .30}, \code{grid = 1} corresponds to cross-loadings of \code{.30},
whereas \code{grid = 2} corresponds to cross-loadings of \code{.60}.

With \code{vary = "multiplier"}, the grid values multiply the originally supplied
overlap values. For example, if \code{cA = cB = .30}, then \code{grid = 1} evaluates the
original overlap and \code{grid = 2} evaluates cross-loadings of \code{.60}.

With \code{vary = "common"}, all non-zero overlap values are replaced by each grid
value. This is useful when researchers want to ask how large the overlap would
have to be, in absolute cross-loading units, for conclusions to change.

With \code{vary = "item"}, only one item-specific cross-loading is varied. This is
useful when uncertainty concerns a particular item rather than the full overlap
structure.

When confidence intervals are requested, the output identifies the first grid
value at which the corrected confidence interval includes zero. This can help
evaluate how strong the assumed overlap would need to be for the corrected
association to no longer be distinguishable from zero.
}
\section{Methods}{

Objects of class \code{"rho_overlap_sensitivity"} have the following methods:
\describe{
\item{\code{print(x)}}{Displays a summary including the observed overlap,
critical overlap (if available), and whether the confidence interval
includes zero.}
\item{\code{plot(x)}}{Plots the corrected association as a function of the
overlap value, optionally including confidence interval bands and markers
for observed and critical overlap.}
}
}

\examples{
mod <- reliability_model(
  relA = .80,
  relB = .85,
  kA = 6,
  kB = 6
)

## Sensitivity to multiplying the assumed overlap
sens <- rho_overlap_sensitivity(
  r_obs = .60,
  type = "continuous",
  measurement_model = mod,
  cA = .30,
  cB = .30,
  k_overlap = 1,
  ci = "analytic",
  r_SE = .10,
  grid = seq(0, 2, by = .25),
  show_progress = FALSE
)

head(sens$results)

## Plot the sensitivity curve
plot(sens)

## Use reported confidence limits for the observed correlation
sens_rci <- rho_overlap_sensitivity(
  r_obs = .60,
  type = "continuous",
  measurement_model = mod,
  cA = .30,
  cB = .30,
  k_overlap = 1,
  ci = "analytic",
  r_ci = c(.45, .72),
  grid = seq(0, 2, by = .25),
  show_progress = FALSE
)

head(sens_rci$results)

#' ## Sensitivity by replacing all non-zero overlap values
sens_common <- rho_overlap_sensitivity(
  r_obs = .60,
  type = "continuous",
  measurement_model = mod,
  cA_vec = c(0, 0, 0, 0, .30, .20),
  cB_vec = c(.25, .15, 0, 0, 0, 0),
  ci = "analytic",
  r_SE = .10,
  vary = "common",
  grid = seq(0, .60, by = .10),
  show_progress = FALSE
)

head(sens_common$results)

## Sensitivity for one item-specific overlap parameter
sens_item <- rho_overlap_sensitivity(
  r_obs = .60,
  type = "continuous",
  measurement_model = mod,
  cA_vec = c(0, 0, 0, 0, .30, .20),
  cB_vec = c(.25, .15, 0, 0, 0, 0),
  ci = "analytic",
  r_SE = .10,
  vary = "item",
  focal_scale = "A",
  focal_item = 5,
  grid = seq(0, .60, by = .10),
  show_progress = FALSE
)

head(sens_item$results)

plot(sens_item)


}
