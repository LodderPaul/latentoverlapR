% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_items_lvm.R
\name{sim_items_lvm}
\alias{sim_items_lvm}
\title{Simulate ordinal item scores from a latent variable model}
\usage{
sim_items_lvm(
  N,
  seeds,
  nfactors,
  nitems,
  loadings,
  intercepts = 0,
  residuals = NA,
  threshold = 0,
  latents = NA,
  latcor = 0,
  latskew = 0,
  latkurt = 0,
  skew_multivar = TRUE,
  plots = TRUE
)
}
\arguments{
\item{N}{Number of observations to generate.}

\item{seeds}{Random seed used for reproducibility.}

\item{nfactors}{Number of latent factors.}

\item{nitems}{Number of items per factor. Either a scalar, recycled across
factors, or a vector of length \code{nfactors}.}

\item{loadings}{A list of loading matrices, one per factor. Each element
should be a matrix with \code{nfactors} rows and \code{nitems[f]} columns. Rows
represent latent factors and columns represent items. Cross-loadings can be
specified by non-zero off-factor entries.}

\item{intercepts}{Item intercepts. Can be a scalar, a vector of length
\code{nfactors}, or a list of length \code{nfactors} containing scalars or item-level
vectors.}

\item{residuals}{Residual variance or covariance specification. Can be \code{NA}
for independent residuals with variance 1, a scalar residual variance, a
vector of residual variances per factor or per item, a list of residual
variances per factor, or a full residual covariance matrix. A full matrix can
be used to generate residual covariances between items.}

\item{threshold}{Threshold value(s) used to discretize continuous item scores
into ordinal item scores. A scalar threshold creates two categories coded
\code{0} and \code{1}; a vector of thresholds creates \code{length(threshold) + 1}
categories coded from \code{0} to \code{length(threshold)}.}

\item{latents}{Optional matrix of latent factor scores. If supplied, these are
used instead of generating new latent scores.}

\item{latcor}{Latent factor correlation specification. Use scalar \code{0} for
independent factors, or provide a full \code{nfactors} by \code{nfactors} correlation
matrix.}

\item{latskew}{Skewness parameter(s) used when generating skewed latent
variables. Can be scalar or a vector of length \code{nfactors}.}

\item{latkurt}{Kurtosis parameter(s) used when generating non-normal latent
variables with \code{monte1()}. Can be scalar or a vector of length \code{nfactors}.}

\item{skew_multivar}{Logical. If \code{TRUE} and \code{nfactors > 1}, the function tries
to use \code{monte1()} to generate multivariate non-normal latent variables with
the requested skewness, kurtosis, and latent correlation structure. If
\code{FALSE}, multivariate normal latent variables are generated.}

\item{plots}{Logical. If \code{TRUE}, histograms and scatterplot matrices are
produced for the generated latent variables and item scores.}
}
\value{
A list with four elements:
\describe{
\item{Ordinal}{A list of data frames containing the generated ordinal item
scores for each factor.}
\item{Continuous}{A list of matrices containing the underlying continuous
item scores for each factor.}
\item{Latents}{A matrix containing the generated or supplied latent factor
scores.}
\item{Sigma_e}{The residual covariance matrix used to generate residuals.}
}
}
\description{
Generates continuous and ordinal item scores from a latent variable model with
one or more latent factors. The function supports primary loadings,
cross-loadings, correlated latent variables, skewed latent variables, and
correlated residuals. It is mainly intended for simulation studies,
demonstrations.
}
\details{
The continuous item scores are generated as

\deqn{x = \nu + \Lambda \eta + \epsilon,}

where \code{intercepts} define \eqn{\nu}, \code{loadings} define \eqn{\Lambda},
\code{latents} or generated factor scores define \eqn{\eta}, and \code{residuals}
define the residual covariance matrix of \eqn{\epsilon}. Ordinal scores are
then obtained by applying \code{findInterval()} to the continuous item scores using
the supplied thresholds.

Skewed ordinal item distributions can be generated by using asymmetric
thresholds, skewed latent variables, or both.
}
\note{
This function uses \code{mvtnorm}, \code{MASS}, \code{fungible}, and \code{sn}. For package
development, these should preferably be declared in \code{DESCRIPTION} under
\code{Imports}, and functions should be called with explicit namespaces rather than
using \code{require()} inside the function.
}
\examples{
## One-factor model with five ordinal items
load1 <- matrix(rep(.80, 5), nrow = 1)

sim1 <- sim_items_lvm(
  N = 500,
  seeds = 123,
  nfactors = 1,
  nitems = 5,
  loadings = list(load1),
  residuals = .36,
  threshold = c(-1.5, -0.5, 0.5, 1.5),
  plots = FALSE
)

head(sim1$Ordinal[[1]])

## Two correlated factors with one cross-loading
loadA <- matrix(
  c(
    .80, .80, .80, .80,
    0,   0,   0,   .30
  ),
  nrow = 2,
  byrow = TRUE
)

loadB <- matrix(
  c(
    .30, 0,   0,   0,
    .80, .80, .80, .80
  ),
  nrow = 2,
  byrow = TRUE
)

sim2 <- sim_items_lvm(
  N = 500,
  seeds = 123,
  nfactors = 2,
  nitems = c(4, 4),
  loadings = list(loadA, loadB),
  residuals = .36,
  threshold = c(-1.5, -0.5, 0.5, 1.5),
  latcor = matrix(c(1, .30, .30, 1), 2, 2),
  skew_multivar = FALSE,
  plots = FALSE
)

cor(rowSums(sim2$Ordinal[[1]]), rowSums(sim2$Ordinal[[2]]))

}
